<script lang="ts">let { data } = $props(); let q=$state(''); let filtered=$derived(data.items.filter((x:any)=>x.title.includes(q)||x.category.includes(q)));</script>
<svelte:head><title>کتابخانه | ZetaMed</title><meta name="description" content="کتابخانه علمی زتامد؛ پزشکی سلولی مولکولی، ژنتیک، ارتوبیولوژی، IVF، زخم و درمانگاه زانو."/></svelte:head>
<section class="container page-head"><div class="eyebrow">ZETAMED LIBRARY</div><h1>کتابخانه علمی</h1><p>تمام نوشته‌ها و مقالات زتامد در یک نمای سریع، خوانا و قابل جست‌وجو.</p><input bind:value={q} placeholder="جست‌وجو در عنوان یا دسته‌بندی…" aria-label="جست‌وجوی کتابخانه"/></section>
<section class="container list">
{#each filtered as item, i}<a href={item.href}><span class="num">{String(i+1).padStart(2,'0')}</span><div><small>{item.category}</small><h2>{item.title}</h2>{#if item.excerpt}<p>{item.excerpt}</p>{/if}</div><b>↗</b></a>{/each}
</section>
<style>.page-head{padding:6rem 0 3rem}.page-head h1{font-size:clamp(2.5rem,6vw,5rem);margin:.8rem 0}.page-head p{color:var(--muted)}input{margin-top:2rem;width:min(620px,100%);background:#0c0c0f;border:1px solid var(--line);color:#eee;border-radius:14px;padding:1rem 1.1rem;outline:0}input:focus{border-color:rgba(224,188,96,.42)}.list{border-top:1px solid var(--line)}.list>a{display:grid;grid-template-columns:58px 1fr 30px;gap:1rem;align-items:center;padding:1.35rem .4rem;border-bottom:1px solid var(--line);text-decoration:none;color:inherit}.num{font:500 .7rem monospace;color:#5f594d}.list small{font-size:.68rem;color:#8e753d}.list h2{font-size:1rem;font-weight:500;margin:.25rem 0}.list p{color:var(--muted);font-size:.78rem;max-width:800px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.list b{color:#9f7b31;font-weight:400}.list>a:hover{padding-right:.8rem;background:linear-gradient(90deg,rgba(214,169,66,.035),transparent)}</style>
